# 4.2 哈尔滨工业大学2019年机试真题（回忆版）
单项选择题每题2分，共120分，计算机网络部分自2020年以后为初试考试科目，复试不再考察，故此处不再包含

---

## 一、离散数学
1. 下面哪个是永真式（）
A. $(p \to q) \land (q \to p)$
B. $(p \land \neg q) \to p$
C. $(\neg p \lor q) \land \neg(\neg p \land \neg q)$
D. $\neg(p \lor q)$

2. 下列哪一组命题公式不是等价的（）
A. $(p \to q) \land (q \to p),\ p \leftrightarrow q$
B. $\neg(p \leftrightarrow q),\ (p \land \neg q) \lor (\neg p \land q)$
C. $p \to (q \lor r),\ \neg p \land (q \lor r)$
D. $p \to (q \lor r),\ (p \land \neg q) \to r$

3. 设命题公式 $G = \neg(P \to Q),\ H = P \to (Q \to \neg P)$，则$G$与$H$的关系是（）
A. $G \Rightarrow H$
B. $H \Rightarrow G$
C. $G = H$
D. 以上说法都不对

4. 二部图 $K_{(3,3)}$ 是（）
A. 欧拉图
B. 哈密顿图
C. 平面图
D. 完全图

5. 设$R$为实数集，映射 $f: R \to R,\ f(x) = -x^2 + 2x - 1$，则$f$为（）
A. 单射而非满射
B. 满射而非单射
C. 双射
D. 既非单射也非满射

6. 已知某平面图的顶点数为12，边数为14，则该平面图的面数为（）
A. 2
B. 3
C. 4
D. 5

7. 关于循环群与交换群的关系，下列说法正确的是（）
A. 交换群一定是循环群
B. 循环群一定是交换群
C. 循环群不一定是交换群
D. 以上说法都不对

---

## 二、逻辑题
1. 一位酒商共有5桶葡萄酒和1桶啤酒，6个桶的容量分别为30升、32升、36升、38升、40升和62升，并且只卖整桶酒，不零卖。第一位顾客买走了2整桶葡萄酒，第二位顾客买走的葡萄酒是第一位顾客的2倍。那么，本来有多少升啤酒（）
A. 30
B. 36
C. 40
D. 62

2. 有七支队伍参加篮球赛，每个队伍都与其它六支队伍进行了比赛，已知前六个个队伍的胜负情况如下（胜-负）：
队伍1：4-2，队伍2：1-5，队伍3：2-4，队伍4：3-3，队伍5：3-3，队伍6：6-0。则队伍7的胜负情况为（）
A. 1-5
B. 2-4
C. 3-3
D. 4-2

3. 小白、小黄和小蓝一起出去玩，每个人都带了一个小皮包用来装东西。提白色皮包的人发现他们三个有个很有趣的现象：他们三个人的皮包也分别是白色、黄色和蓝色，但是皮包的颜色却和主人的姓氏不一样。小黄听到后，低头看了看，点头说道：“是啊，的确如此。”则小蓝的皮包是什么颜色（）
A. 白色
B. 黄色
C. 蓝色
D. 无法判断

4. 在某足球联赛开赛前，四个球迷有如下断定：
甲：红队必然不会夺冠
乙：红队可能夺冠
丙：如果蓝队夺冠，那么黄队是第三名
丁：冠军是蓝队
如果四人的断定中只有一个为假，则可推出以下哪个结论？（）
A. 冠军是红队
B. 甲的断定为假
C. 乙的断定为真
D. 黄队是第三名

---

## 三、数据库系统
1. 已知关系$R$和关系$S$，若要对 $R(\text{Join on } R.Y=S.Y)S$ 进行代价估计，则下列说法正确的是（）
A. 若 $T(R)=10000,\ T(S)=50000,\ V(R,Y)=500,\ V(S,Y)=1000$，则连接运算的代价估计为500,000
B. 若 $T(R)=10000,\ T(S)=50000,\ V(R,Y)=500,\ V(S,Y)=1000$，则连接运算的代价估计为1,000,000
C. 若 $T(R)=10000,\ T(S)=50000,\ V(R,Y)=1000,\ V(S,Y)=500$，则连接运算的代价估计为1,000,000
D. 若 $T(R)=10000,\ T(S)=50000,\ V(R,Y)=2000,\ V(S,Y)=1000$，则连接运算的代价估计为500,000

2. 已知$BX$为关系$X$的磁盘块数目，$LX$为关系$X$的元组长度，$TX$为关系$X$的元组数目，$b$为磁盘块或内存页的大小，$M$为可用内存页的数目。则以下算法的适用条件为（）
```text
For i = 1 to BR
    read i-th block of R;
Next i
For j = 1 to BS
    read j-th block of S;
For p = 1 to TR
    read p-th record of R;
For q = 1 to b/IS
    read q-th record of S;
    if R.A θ S.B then
        {串接R的p-th record和S的q-th record 存入结果关系}
Next q
Next p
Next j
```
A. $BR < BS,\ BS < M$
B. $BR > BS,\ BS < M$
C. $BS > BR,\ BR < M$
D. $BS < BR,\ BR < M$

3. 关于索引，以下说法正确的是（）
A. 一个主文件上可以建立多个索引，但更新主文件数据时只更新一个索引
B. 一个主文件上只能建立一个索引，无论是更新还是查询都利用该索引
C. 一个主文件上只能有一个可以决定物理存储的索引
D. 一个主文件上可以有一个或多个可以决定物理存储的索引

4. 用B+树建立主索引，下列说法正确的是（）
A. 叶节点某索引项$X$的左侧指针，指向键值$=X$记录所在的索引文件存储块
B. 非叶节点某索引项$X$的左侧指针，指向键值$=X$记录所在的索引文件存储块
C. 叶节点某索引项$X$的左侧指针，指向键值$\le X$记录所在的索引文件存储块
D. 非叶节点某索引项$X$的左侧指针，指向键值$<X$记录所在的索引文件存储块

5. 若要对E-R图（教师 --(班主任)-- 学生）中“班主任”联系进行处理，说法正确的是（）
A. 需要建立一个“班主任”的关系
B. 不需要建立“班主任”的关系，也不需要任何处理
C. 不需要建立“班主任”的关系，但需要做处理，即将“教师”实体的关键字作为“学生”实体对应关系的属性
D. 不需要建立“班主任”的关系，但需要做处理，即将“学生”实体的关键字作为对应关系的属性

6. 给定关系模式 $R(U, F)$，其中 $U = \{A_1, A_2, A_3, A_4, A_5\}$，函数依赖集 $F = \{A_2 \to A_1;\ A_4 \to A_1;\ A_1 \to A_5;\ (A_1, A_3) \to A_2;\}$。有一个分解 $\rho = \{R_1(A_1, A_2, A_3, A_5),\ R_1(A_3, A_4)\}$，则该分解（）
A. 既具有无损连接性，又保持函数依赖
B. 不具有无损连接性，但保持函数依赖
C. 具有无损连接性，但不保持函数依赖
D. 即不具有无损连接性，又不保持函数依赖

7. 在 $R(X, Y)$ 中，如果 $Y \to X$，并且对于 $X$ 的某一个真子集 $X'$，有 $X' \to Y$，则（）
A. Y函数决定X
B. Y对X完全函数依赖
C. X为关系R的超码
D. 关系R属于2NF

8. 已知关系模式 $R(A, B, C)$，函数依赖集 $F = \{B \to C,\ B \to A,\ AB \to C\}$，则关于，说法正确的是（）
A. R不满足第二范式
B. R满足第二范式但不满足第三范式
C. R满足第三范式但不满足Boyce-Codd范式
D. R满足Boyce-Codd范式

9. 已知关系模式 $R(A, B, C, D, E, F, G)$，函数依赖集 $F = \{ADG \to EF,\ CD \to F,\ F \to B,\ A \to D\}$，则能从$F$中推导出的函数依赖是（）
A. $ADG \to E,\ AG \to E$
B. $D \to B,\ G \to E$
C. $CD \to B,\ AC \to B,\ CDG \to F,\ FE \to CB$
D. $F \to B,\ FA \to D,\ A \to EF$

10. 已知关系模式 $R(A, B, C, D, E, F, G)$，函数依赖集 $F = \{AB \to CF,\ AD \to CE,\ AG \to B,\ D \to C,\ B \to D\}$，则$F$的最小覆盖为（）
A. $AB \to C,\ AB \to F,\ AD \to C,\ AD \to E,\ AG \to B,\ B \to D,\ D \to C$
B. $AB \to F,\ AD \to E,\ AG \to B,\ D \to C,\ B \to D$
C. $AB \to F,\ AD \to C,\ AG \to B,\ D \to C,\ B \to D$
D. $A \to F,\ B \to F,\ AD \to E,\ A \to B,\ G \to B,\ D \to C,\ B \to D$

11. 关于用Undo型日志进行数据库恢复，下列说法正确的是（）
A. 用Undo型日志恢复是对已完成的事务，将日志记录的新值写回磁盘；而对未完成的事务，将日志记录的旧值写回磁盘
B. 用Undo型日志恢复是对已完成的事务，将日志记录的旧值写回磁盘；而对未完成的事务，将日志记录的新值写回磁盘
C. 用Undo型日志恢复是对已完成的事务，跳过；而对未完成的事务，将日志记录的值写回磁盘
D. 用Undo型日志恢复是对已完成的事务，将日志记录的值写回磁盘；而对未完成的事务，跳过

12. 关于关系运算，下列说法正确的是（）
A. 安全的元组演算公式和安全的域演算公式可以等价交换
B. 关系代数运算和关系演算之间是互补的，即它们可以利用各自特点解决彼此不能解决的问题
C. 关系代数和关系演算不一定是安全的
D. 关系演算以属性为基本单位处理单位

---

## 四、C语言程序设计
1. **编程题**：用`gets()`读入一串长度不超过200的字符串，单词之间用空格符隔开，要求统计单词个数，并将单词分离，按字母表降序输出。
**实例**
输入：
```text
i have a dream
```
输出：
```text
num: 4
i
have
dream
a
```

2. **改错题**：学生成绩管理系统：输入学生的学号成绩，按照成绩降序输出学生信息。考察结构体与指针。

3. **改错题**：击鼓传花的游戏，游戏规则：$n$个同学坐着围成一个圆圈，指定一个同学手里拿着一束花，主持人在旁边背对着大家开始击鼓，鼓声开始之后拿着花的同学开始传花，每个同学都可以把花传给自己左右的两个同学中任意的一个，当主持人停止击鼓时，传花停止，此时，正拿着花没传出去的那个同学就要给大家表演一个节目。
请问有多少种传递方法使得从小明手里开始传递的花，传了$m$次后，又回到小明手里。比如有3个同学：1号、2号、3号，并假设小明为1号，花传了3次回到小明手里的方法有：1号→2号→3号→1号；1号→3号→2号→1号，两种方式。

4. **改错题**：质因数分解。例如：$100 = 2 * 2 * 5 * 5$。这道题是苏小红老师书上原题。