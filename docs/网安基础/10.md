---
title: 反射型拒绝服务攻击（放大攻击 NTP DNS Chargen）

tags:

---

## 反射型拒绝服务攻击（放大攻击 NTP DNS Chargen）
反射型拒绝服务攻击**不需要控制大量僵尸网络**，攻击者利用**反射器**（高性能服务器或多个服务器）间接向目标主机发送大量数据包。反射节点无法识别发起源是否具有恶意动机，攻击者会选用**应答数据远大于请求包**的应用层协议（如 **DNS、NTP、Chargen、SSDP**），从而实现**流量放大**效果，耗尽目标主机资源。
