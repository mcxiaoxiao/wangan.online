---
title: 2023年哈尔滨工业大学计算机复试机试真题
---

# 2023年哈尔滨工业大学计算机复试机试真题（回忆版）
## 一、离散数学

1. 映射$f:X→Y$，$g:Y→X$，满足$g \circ f=I_X$，说法正确的是（）
- A. f 一定可逆
- B. f 一定不可逆
- C. f 可能可逆
- D. 以上都不对

2. 某图共15个顶点，21条边，顶点度数只能为3、4、5、6。其中度数为4的顶点有2个，度数为6的顶点有3个，则度数为5的顶点有（）个。

3. 已知$x-y=a+bi$，则 x 与 y 满足（）关系。

4. 群中元素逆元与自身相等，该群为（）群。

5. 群中有元素 a、b、c，满足$a*a=b$，$b*b=c$，$c*c=a$，说法正确的是（）
- A. a 一定是单位元
- B. b 一定是单位元
- C. c 一定是单位元
- D. $a*b*c$ 一定是单位元

6. 以下哪个图不是平面图（正确答案：$K_{3,3}$）

7. 考察范式定义：极小项、极大项、主析取范式、主合取范式。

8. 求一个图的最短路径（手动画图求解）。

9. 使用$A^*$算法求解三个修道士与三个狼人过河问题。

---

## 二、数据库系统

1. 关于游标使用，正确的编译顺序是（）
(1) exec sql open cur student;
(2) exec sql fetch cur student into :vSno, :vSname, :vSclass;
(3) exec sql declare cur student cursor for select Sno,Sname,Sclass from Student where Sclass='035101';
(4) exec sql close cur student;
- A. (1)(2)(3)(4)
- B. (3)(1)(2)(4)
- C. (3)(2)(1)(4)
- D. (1)(3)(2)(4)

2. 给定SQL程序段与goto语句，关于错误处理说法正确的是（）
    ......
    ...X...
    ......
    goto s1;
    exec sql whenever sqlerror continue;
    ......
    ...Y...
    ......
    s1: ...Z...
- A. 如果由 Y 到 Z 区域执行发生错误，则 continue（继续向下执行）
- B. 如果 X 区域发生错误，则 continue（继续向下执行）
- C. 如果由 Y 到 Z 区域执行发生错误，则跳转到 stop 标号处继续执行
- D. 如果 X 区域发生错误，则跳转到 stop 标号处继续执行

3. 已知如下关系：学生`Student(S#, Sname)`，课程`Course(C#, Cname, T#)`，选课`SC(S#, C#, Score)`，教师`T(T#, Tname)`。其中`S#`为学号，`Sname`为学生名，`C#`为课号，`Cname`为课程名，`T#`为教师编号，`Score`为成绩，`Tname`为教师名。


    ```sql
    SELECT S# FROM SC WHERE Score<60 AND Count(*)>2 GROUP BY S#;
    ```

    关于其查询结果，以下说法正确的是（）
    A. 其它选项的说法都不正确
    B. 有2门以上不及格课程的同学的学号
    C. 有2门以上及格课程的同学的学号
    D. 有2人以上不及格课程的同学的学号

4. Student表50人，45人有成绩，10人不及格。
SELECT * FROM Student WHERE EXISTS (SELECT * FROM SC WHERE Score<60);
返回（）条记录。
A.50
B.45
C.10
D.35

5. 已知关系 $R\{A, B, C, D, E, F, G\}$，函数依赖集 $F$ 为 $\{AB \to CF,\ AD \to CE,\ AG \to B,\ D \to C,\ B \to D\}$。则 $F$ 的最小覆盖为（ ）

    A. $\{AB \to C,\ AB \to F,\ AD \to E,\ AG \to B,\ B \to D,\ D \to C\}$

    B. $\{AB \to F,\ AD \to E,\ AG \to B,\ D \to C,\ B \to D\}$

    C. $\{AB \to F,\ AD \to C,\ AG \to B,\ D \to C,\ B \to D\}$

    D. $\{A \to F,\ B \to F,\ AD \to E,\ A \to B,\ G \to B,\ D \to C,\ B \to D\}$

6. 基于时间戳的并发控制，不需要锁，但需要进行冲突检测，当发生冲突时需要撤销事务并重启事务以解决冲突。已知$T_1$，$T_2$，$T_3$三个事务，记$w_i(A)$为事务$T_i$写数据对象$A$，$r_i(A)$为事务$T_i$读数据对象$A$。$T_1$，$T_2$，$T_3$三个事务的时间戳为200，150和180。三个事务的操作依照下列次序进行：
$r_1(B);\ r_2(A);\ r_3(C);\ w_1(B);\ w_1(A);\ w_2(C);\ w_3(A);$

    则被撤销的事务为（ ）
    A. $T_1$被撤销/重启，$T_2$被撤销/重启
    B. $T_2$被撤销/重启，$T_3$被撤销/重启
    C. $T_1$被撤销/重启，$T_3$被撤销/重启
    D. 没有被撤销/重启的事务

7. $T_1$，$T_2$是两个事务，图(a)(b)(c)给出这两个事务的三种调度，这三种调度会出现什么问题，正确的选项是（）

    | 图(a)调度 |       |
    | :--- | :--- |
    | $T_1$ | $T_2$ |
    | Read A | |
    | Update A | |
    | | Read A |
    | | Update A |
    | Write A | |
    | | Write A |

    | 图(b)调度 |       |
    | :--- | :--- |
    | $T_1$ | $T_2$ |
    | Read A | |
    | | Read A |
    | | Update A |
    | | Write A |
    | Read A | |

    | 图(c)调度 |       |
    | :--- | :--- |
    | $T_1$ | $T_2$ |
    | | Read A |
    | | Update A |
    | | Write A |
    | Read A | |
    | | Roll Back |
    | Read A | |

    A. 图(a)的调度会出现“丢失修改”，图(b)的调度会出现“重复读错误”，图(c)的调度会出现“脏读”
    B. 图(a)的调度会出现“脏读”，图(b)的调度会出现“重复读错误”，图(c)的调度会出现“丢失修改”
    C. 图(a)的调度会出现“重复读错误”，图(b)的调度会出现“脏读”，图(c)的调度会出现“丢失修改”
    D. 图(a)的调度会出现“丢失修改”，图(b)的调度会出现“脏读”，图(c)的调度会出现“重复读错误”

8. $T_1$，$T_2$是两个事务，图(a)(b)(c)给出这两个事务的三种调度，这三种调度会出现什么问题，正确的选项是（）

    | 图(a)调度 |       |
    | :--- | :--- |
    | $T_1$ | $T_2$ |
    | Read A | |
    | | Read A |
    | Update A | |
    | Write A | Update A |
    | Roll Back | |
    | | Write A |

    | 图(b)调度 |       |
    | :--- | :--- |
    | $T_1$ | $T_2$ |
    | Read A | |
    | | Read A |
    | | Update A |
    | | Write A |
    | Read A | |

    | 图(c)调度 |       |
    | :--- | :--- |
    | $T_1$ | $T_2$ |
    | Read A | |
    | | Read A |
    | | Update A |
    | | Write A |
    | | Roll Back |
    | Read A | |

    A. 图(a)的调度会出现“丢失修改”，图(b)的调度会出现“重复读错误”，图(c)的调度会出现“脏读”
    B. 图(a)的调度会出现“脏读”，图(b)的调度会出现“重复读错误”，图(c)的调度会出现“丢失修改”
    C. 图(a)的调度会出现“重复读错误”，图(b)的调度会出现“脏读”，图(c)的调度会出现“丢失修改”
    D. 以上选项均不正确

9. 已知事务 $T_1, T_2, T_3$，记 $w_i(A)$ 为事务 $T_i$ 写数据对象 $A$，$r_i(A)$ 为事务 $T_i$ 读数据对象 $A$。

    有两个调度 $S_1$ 和 $S_2$ 分别为：

    *   **调度 $S_1$**：
        $r_2(A);\ r_1(B);\ w_2(A);\ r_2(B);\ w_3(A);\ w_2(B);$

    *   **调度 $S_2$**：
        $r_2(A);\ r_1(B);\ w_2(A);\ r_3(A);\ w_1(B);\ r_2(B);\ w_2(B);$

    关于 $S_1$ 和 $S_2$，说法正确的是（ ）

    A. $S_1$ 是冲突可串行化调度，$S_2$ 是冲突可串行化调度
    B. $S_1$ 是非冲突可串行化调度，$S_2$ 是冲突可串行化调度
    C. $S_1$ 是冲突可串行化调度，$S_2$ 是非冲突可串行化调度
    D. $S_1$ 是非冲突可串行化调度，$S_2$ 是非冲突可串行化调度

10. 已知 $T_1, T_2, T_3$ 三个事务，记 $w_i(A)$ 为事务 $T_i$  写数据对象 $A$，$r_i(A)$ 为事务 $T_i$  读数据对象 $A$。

    有两个调度 $S_1$ 和 $S_2$ 分别为：
    - $S_1$: $r_2(A); r_1(B); w_2(A); r_2(B); r_3(A); w_1(B); w_3(A); w_2(B);$
    - $S_2$: $r_2(A); r_1(B); w_2(A); r_3(A); w_1(B); w_3(A); r_2(B); w_2(B);$

    关于 $S_1$ 和 $S_2$，说法正确的是（ ）
    A. $S_1$ 是冲突可串行化调度，$S_2$ 是冲突可串行化调度
    B. $S_1$ 是非冲突可串行化调度，$S_2$ 是冲突可串行化调度
    C. $S_1$ 是冲突可串行化调度，$S_2$ 是非冲突可串行化调度
    D. $S_1$ 是非冲突可串行化调度，$S_2$ 是非冲突可串行化调度

11. 考察Undo、Redo日志恢复。

---

## 三、算法设计与分析

1. 快速排序最好情况下的时间复杂度为（）
A.$O(nlog_2n)$
B.$O(n^2)$
C.$O(n)$
D.$O(log_2n)$

---

## 四、C语言程序设计

### 1. 编程题：员工信息筛选排序
结构体定义：
```c
typedef struct{
    char name[20];
    int score[3];
    float sum;
    float aver;
    int num; // 缺席次数
}Worker;
```
要求：
1. 输入n个员工信息
2. 筛选：平均分≥85 且 缺勤≤2次
3. 按姓名字典序**逆序**输出

### 2. 编程题：肥料采购方案
红、黄、紫肥料单价：80、50、35元/袋。
输入经费n（1000<n<2000），输出所有满足经费的购买方案，按红、黄、紫顺序输出。

### 3. 程序改错题：分贺卡问题
n个同学排成一排，满足：
1. 每人至少1张
2. 相邻同学分数高的贺卡多1张
求最少贺卡总数，改正核心代码错误。
